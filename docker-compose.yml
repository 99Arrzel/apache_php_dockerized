services:
  php:
    restart: unless-stopped
    build:
      context: .
      dockerfile: ${DOCKERFILE}
    volumes:
      # - ../../../:/var/www/html
      - ./:/var/www/html
    ports:
      - ${APACHE_PORT}:80